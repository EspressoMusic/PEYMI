*** Begin Patch
*** Update File: C:\Users\Nirhdhd\bakery_shop_app\lib\main.dart
@@
   @override
   Widget build(BuildContext context) {
+    const comboImages = ['☕ + 🍰', '🥐 + 🧃', '🍕 + 🥤', '🍪 + ☕'];
     return Column(
       children: [
         Expanded(
           child: GridView.builder(
             padding: const EdgeInsets.fromLTRB(12, 14, 12, 8),
@@
                 padding: const EdgeInsets.all(10),
                 child: Column(
                   crossAxisAlignment: CrossAxisAlignment.start,
                   children: [
-                    Container(
-                      decoration: BoxDecoration(
-                        color: const Color(0xFFF1E9D8),
-                        borderRadius: BorderRadius.circular(14),
+                    Row(
+                      children: [
+                        Expanded(
+                          child: Container(
+                            decoration: BoxDecoration(
+                              color: const Color(0xFFF1E9D8),
+                              borderRadius: BorderRadius.circular(14),
+                            ),
+                            padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 10),
+                            alignment: Alignment.center,
+                            child: Text(
+                              comboImages[index % comboImages.length],
+                              style: const TextStyle(fontSize: 24),
+                            ),
+                          ),
+                        ),
+                        const SizedBox(width: 8),
+                        Container(
+                          padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 6),
+                          decoration: BoxDecoration(
+                            color: Colors.orange.shade100,
+                            borderRadius: BorderRadius.circular(10),
+                          ),
+                          child: Text(
+                            'עד ${deal['valid']!}',
+                            style: const TextStyle(fontSize: 11, fontWeight: FontWeight.w800),
+                          ),
+                        ),
+                      ],
+                    ),
+                    const SizedBox(height: 8),
+                    const Text(
+                      'הטבה',
+                      style: TextStyle(fontWeight: FontWeight.w900, fontSize: 14),
+                    ),
+                    const SizedBox(height: 4),
+                    Text(
+                      deal['title']!,
+                      maxLines: 2,
+                      overflow: TextOverflow.ellipsis,
+                      style: const TextStyle(fontWeight: FontWeight.w800, fontSize: 15),
+                    ),
+                    const SizedBox(height: 4),
+                    Text(
+                      deal['desc']!,
+                      maxLines: 2,
+                      overflow: TextOverflow.ellipsis,
+                      style: TextStyle(fontSize: 13, color: Colors.brown.shade500),
+                    ),
+                  ],
+                ),
+              );
+            },
+          ),
+        ),
+      ],
+    );
+  }
+}
-                      ),
-                      padding: const EdgeInsets.all(8),
-                      child: const Icon(Icons.discount, color: Colors.deepOrange, size: 24),
-                    ),
-                    const SizedBox(height: 8),
-                    Text(
-                      deal['title']!,
-                      maxLines: 2,
-                      overflow: TextOverflow.ellipsis,
-                      style: const TextStyle(fontWeight: FontWeight.w800, fontSize: 15),
-                    ),
-                    const SizedBox(height: 4),
-                    Text(
-                      deal['desc']!,
-                      maxLines: 2,
-                      overflow: TextOverflow.ellipsis,
-                      style: TextStyle(fontSize: 13, color: Colors.brown.shade500),
-                    ),
-                    const Spacer(),
-                    Text(
-                      'תוקף: ${deal['valid']!}',
-                      maxLines: 1,
-                      overflow: TextOverflow.ellipsis,
-                      style: TextStyle(fontSize: 12, color: Colors.brown.shade400),
-                    ),
-                  ],
-                ),
-              );
-            },
-          ),
-        ),
-        Padding(
-          padding: const EdgeInsets.fromLTRB(12, 4, 12, 12),
-          child: SizedBox(
-            width: double.infinity,
-            child: FilledButton.icon(
-              onPressed: () {
-                ScaffoldMessenger.of(context).showSnackBar(
-                  const SnackBar(content: Text('ההטבה הופעלה בהצלחה!')),
-                );
-              },
-              icon: const Icon(Icons.card_giftcard),
-              label: const Text('קבל הטבה'),
-            ),
-          ),
-        ),
-      ],
-    );
-  }
-}
*** End Patch
