*** Begin Patch
*** Update File: C:\Users\Nirhdhd\bakery_shop_app\lib\main.dart
@@
 class _BakeryHomePageState extends State<BakeryHomePage> {
   int _selectedIndex = 3;
   final Map<String, int> _cartQuantities = {};
   final AudioPlayer _cartSoundPlayer = AudioPlayer();
+  final Set<String> _redeemedDealTitles = <String>{};
@@
       _DealsPage(
         deals: _deals,
+        isDealRedeemed: (deal) => _redeemedDealTitles.contains(deal['title']),
+        onAlreadyRedeemedTap: () {
+          ScaffoldMessenger.of(context).showSnackBar(
+            const SnackBar(content: Text('הדיל כבר מומש, לא ניתן לממש שוב')),
+          );
+        },
         onRedeemDeal: (deal) {
           setState(() {
+            _redeemedDealTitles.add(deal['title'].toString());
             _dealOrders.add({
               'id': 'DEAL-${_dealOrders.length + 1}',
               'title': deal['title'],
               'total': deal['priceAfterDiscount'],
@@
 class _DealsPage extends StatelessWidget {
-  const _DealsPage({required this.deals, required this.onRedeemDeal});
+  const _DealsPage({
+    required this.deals,
+    required this.onRedeemDeal,
+    required this.isDealRedeemed,
+    required this.onAlreadyRedeemedTap,
+  });
 
   final List<Map<String, dynamic>> deals;
   final void Function(Map<String, dynamic> deal) onRedeemDeal;
+  final bool Function(Map<String, dynamic> deal) isDealRedeemed;
+  final VoidCallback onAlreadyRedeemedTap;
@@
             itemBuilder: (context, index) {
               final deal = deals[index];
               final images = (deal['images'] as List).cast<String>();
-              return Container(
-                decoration: BoxDecoration(
-                  color: const Color(0xFFF8F4EC),
-                  borderRadius: BorderRadius.circular(20),
-                  boxShadow: const [
-                    BoxShadow(color: Color(0x22000000), blurRadius: 10, offset: Offset(0, 4)),
-                  ],
-                ),
-                padding: const EdgeInsets.all(10),
-                child: Column(
-                  crossAxisAlignment: CrossAxisAlignment.start,
-                  children: [
+              final redeemed = isDealRedeemed(deal);
+              return InkWell(
+                borderRadius: BorderRadius.circular(20),
+                onTap: redeemed ? onAlreadyRedeemedTap : null,
+                child: AnimatedOpacity(
+                  duration: const Duration(milliseconds: 200),
+                  opacity: redeemed ? 0.72 : 1,
+                  child: Container(
+                    decoration: BoxDecoration(
+                      color: redeemed ? const Color(0xFFE0D8CC) : const Color(0xFFF8F4EC),
+                      borderRadius: BorderRadius.circular(20),
+                      boxShadow: const [
+                        BoxShadow(color: Color(0x22000000), blurRadius: 10, offset: Offset(0, 4)),
+                      ],
+                    ),
+                    padding: const EdgeInsets.all(10),
+                    child: Column(
+                      crossAxisAlignment: CrossAxisAlignment.start,
+                      children: [
                     SizedBox(
                       height: 102,
                       child: Row(
@@
                     Text(
                       'אחרי הנחה: ${deal['priceAfterDiscount']}',
                       textAlign: TextAlign.right,
                       style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 20),
                     ),
                     const SizedBox(height: 6),
                     SizedBox(
                       width: double.infinity,
                       child: FilledButton(
-                        onPressed: () => onRedeemDeal(deal),
-                        child: const Text('מימוש דיל'),
+                        onPressed: redeemed ? null : () => onRedeemDeal(deal),
+                        child: Text(redeemed ? 'מומש' : 'מימוש דיל'),
                       ),
                     ),
-                  ],
+                      ],
+                    ),
+                  ),
                 ),
               );
             },
*** End Patch
