*** Begin Patch
*** Update File: C:\Users\Nirhdhd\bakery_shop_app\lib\main.dart
@@
   @override
   Widget build(BuildContext context) {
-    final textScale = MediaQuery.textScalerOf(context).scale(1);
-    final dealCardHeight = (textScale > 1.15) ? 320.0 : 285.0;
     return Column(
       children: [
         Expanded(
-          child: GridView.builder(
+          child: ListView.separated(
             padding: const EdgeInsets.fromLTRB(12, 14, 12, 8),
             itemCount: deals.length,
-            gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
-              crossAxisCount: 1,
-              crossAxisSpacing: 0,
-              mainAxisSpacing: 10,
-              mainAxisExtent: dealCardHeight,
-            ),
+            separatorBuilder: (_, __) => const SizedBox(height: 10),
             itemBuilder: (context, index) {
               final deal = deals[index];
               final images = (deal['images'] as List).cast<String>();
               final redeemed = isDealRedeemed(deal);
               return InkWell(
@@
                     padding: const EdgeInsets.all(10),
                     child: Column(
                       crossAxisAlignment: CrossAxisAlignment.start,
                       children: [
-                    SizedBox(
-                      height: 102,
-                      child: Row(
-                        children: images.take(2).map((path) {
-                          return Expanded(
-                            child: Container(
-                              margin: const EdgeInsets.symmetric(horizontal: 3),
-                              decoration: BoxDecoration(
-                                color: const Color(0xFFF1E9D8),
-                                borderRadius: BorderRadius.circular(12),
-                              ),
-                              clipBehavior: Clip.antiAlias,
-                              child: Image.asset(path, fit: BoxFit.cover),
-                            ),
-                          );
-                        }).toList(),
-                      ),
-                    ),
-                    const SizedBox(height: 8),
-                    Align(
-                      alignment: Alignment.centerRight,
-                      child: Container(
-                        padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
-                        decoration: BoxDecoration(
-                          color: Colors.orange.shade100,
-                          borderRadius: BorderRadius.circular(10),
-                        ),
-                        child: Text(
-                          'עד ${deal['valid']}',
-                          maxLines: 1,
-                          overflow: TextOverflow.ellipsis,
-                          style: const TextStyle(fontSize: 13, fontWeight: FontWeight.w900),
-                        ),
-                      ),
-                    ),
-                    const SizedBox(height: 8),
-                    const Text(
-                      'הטבה',
-                      textAlign: TextAlign.right,
-                      style: TextStyle(fontWeight: FontWeight.w900, fontSize: 15, color: Color(0xFF5D4037)),
-                    ),
-                    const SizedBox(height: 4),
-                    Text(
-                      deal['title'].toString(),
-                      maxLines: 2,
-                      overflow: TextOverflow.ellipsis,
-                      textAlign: TextAlign.right,
-                      style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 26, height: 1.15),
-                    ),
-                    const SizedBox(height: 4),
-                    Text(
-                      deal['desc'].toString(),
-                      maxLines: 2,
-                      overflow: TextOverflow.ellipsis,
-                      textAlign: TextAlign.right,
-                      style: TextStyle(fontSize: 16, height: 1.25, color: Colors.brown.shade600),
-                    ),
-                    const Spacer(),
-                    Text(
-                      'אחרי הנחה: ${deal['priceAfterDiscount']}',
-                      textAlign: TextAlign.right,
-                      style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 20),
-                    ),
-                    const SizedBox(height: 6),
-                    SizedBox(
-                      width: double.infinity,
-                      child: FilledButton(
-                        onPressed: redeemed ? null : () => onRedeemDeal(deal),
-                        child: Text(redeemed ? 'מומש' : 'מימוש דיל'),
-                      ),
-                    ),
+                        SizedBox(
+                          height: 110,
+                          child: Row(
+                            children: images.take(2).map((path) {
+                              return Expanded(
+                                child: Container(
+                                  margin: const EdgeInsets.symmetric(horizontal: 3),
+                                  decoration: BoxDecoration(
+                                    color: const Color(0xFFF1E9D8),
+                                    borderRadius: BorderRadius.circular(12),
+                                  ),
+                                  clipBehavior: Clip.antiAlias,
+                                  child: Image.asset(path, fit: BoxFit.cover),
+                                ),
+                              );
+                            }).toList(),
+                          ),
+                        ),
+                        const SizedBox(height: 8),
+                        Align(
+                          alignment: Alignment.centerRight,
+                          child: Container(
+                            padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
+                            decoration: BoxDecoration(
+                              color: Colors.orange.shade100,
+                              borderRadius: BorderRadius.circular(10),
+                            ),
+                            child: Text(
+                              'עד ${deal['valid']}',
+                              style: const TextStyle(fontSize: 13, fontWeight: FontWeight.w900),
+                            ),
+                          ),
+                        ),
+                        const SizedBox(height: 8),
+                        Text(
+                          deal['title'].toString(),
+                          textAlign: TextAlign.right,
+                          style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 23, height: 1.2),
+                        ),
+                        const SizedBox(height: 6),
+                        Text(
+                          deal['desc'].toString(),
+                          textAlign: TextAlign.right,
+                          style: TextStyle(fontSize: 16, height: 1.3, color: Colors.brown.shade600),
+                        ),
+                        const SizedBox(height: 12),
+                        Text(
+                          'אחרי הנחה: ${deal['priceAfterDiscount']}',
+                          textAlign: TextAlign.right,
+                          style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 19),
+                        ),
+                        const SizedBox(height: 8),
+                        SizedBox(
+                          width: double.infinity,
+                          child: FilledButton(
+                            onPressed: redeemed ? null : () => onRedeemDeal(deal),
+                            child: Text(redeemed ? 'מומש' : 'מימוש דיל'),
+                          ),
+                        ),
+                        if (redeemed) ...[
+                          const SizedBox(height: 6),
+                          Text(
+                            'הדיל כבר מומש',
+                            textAlign: TextAlign.center,
+                            style: TextStyle(color: Colors.brown.shade500, fontWeight: FontWeight.w800),
+                          ),
+                        ],
                       ],
                     ),
                   ),
                 ),
*** End Patch
